{
  "options": {"step": {"init": {"title": "SurvNG options", "data": {"scan_interval": "Reconciliation interval (seconds)", "stream_source": "Preferred stream"}, "data_description": {"stream_source": "Live uses the camera substream when configured; Main uses the high-resolution stream."}}}},
  "issues": {"legacy_mqtt_discovery": {"title": "Disable duplicate SurvNG MQTT discovery", "description": "{entry_title} is publishing legacy Home Assistant MQTT discovery while the SurvNG integration owns the same devices. Disable Home Assistant Discovery in SurvNG Admin > General > API, then remove the duplicate MQTT-discovered entities."}},
  "config": {
    "step": {
      "user": {"title": "Connect to SurvNG", "description": "Create a read + camera-control token in SurvNG Admin > General > API.", "data": {"url": "SurvNG URL", "api_token": "API token", "verify_ssl": "Verify TLS certificate", "mqtt_prefix": "MQTT topic prefix"}},
      "reauth_confirm": {"title": "Update SurvNG API token", "data": {"api_token": "API token"}},
      "reconfigure": {"title": "Reconfigure SurvNG", "data": {"url": "SurvNG URL", "verify_ssl": "Verify TLS certificate", "mqtt_prefix": "MQTT topic prefix"}}
    },
    "error": {"cannot_connect": "Unable to connect to SurvNG", "invalid_auth": "The API token is invalid or lacks required scopes"},
    "abort": {"already_configured": "This SurvNG server is already configured", "reauth_successful": "Authentication updated"}
  }
}
